services:
   app:
       build: .
       command: bash -c "python src/preprocess.py && python src/train.py && python src/predict.py -m LOG_REG -t func && coverage run src/unit_tests/test_preprocess.py && coverage report -m"
       ports:
           - 8080:8080
       image: derelia/lab_itmo_bigData:latest